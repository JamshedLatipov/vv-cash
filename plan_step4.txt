Okay, the `products.Category` definition is:
{
    "id": "string",
    "name": "string",
    "code": "string",
    "show_on_cash": boolean,
    ...
}

The `/cashes/category/` and `/cashes/category/show-on-cash/` endpoints return a Response with a `List` structure where `body` contains an array of categories (probably). Wait, what does `response.Response` look like?

Let's check `response.Response` schema.
Actually, wait, in `AuthService.cs` we saw how they deserialize APIs. Let's look at it.
