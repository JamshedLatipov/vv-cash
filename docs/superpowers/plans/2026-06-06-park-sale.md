# Отложенный чек (Park Sale) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать кассиру отложить текущий чек (товары + скидки + клиент) в локальное SQLite-хранилище, держать список отложенных и возвращать любой обратно в корзину.

**Architecture:** Зеркалим существующий offline-паттерн (`ExpenseDocumentService` → JSON → таблица `UnsyncedDocuments` в `OfflineStorageService`). Снимок корзины сериализуется в JSON и хранится в новой таблице `ParkedSales`. Логика — в новом singleton-сервисе `ParkedSaleService`. UI: оживляем мёртвую кнопку «Заказы»→«Отложенные», добавляем кнопку «Отложить» и окно списка по образцу `CustomerSearchWindow`.

**Tech Stack:** C# / .NET 8, Avalonia UI, CommunityToolkit.Mvvm (source-gen `[ObservableProperty]`/`[RelayCommand]`), Microsoft.Data.Sqlite, System.Text.Json.

**Спека:** [docs/superpowers/specs/2026-06-06-park-sale-design.md](../specs/2026-06-06-park-sale-design.md)

---

## Замечания по верификации

В репозитории **нет тест-проекта** — верификация ручная (см. Task 9). Сборка для проверки компиляции на каждом шаге.

**ВАЖНО (file lock):** если приложение запущено, сборка в обычный `bin/` падает из-за блокировки файла. Собирать в отдельную папку:

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```

Ожидаемо: `Build succeeded. 0 Error(s)`. Эту команду далее зовём «**verify-build**».

---

## Структура файлов

**Новые:**
- `src/VvCash/Models/ParkedSale.cs` — строка списка (денормализовано) + `ParkedCartItem`.
- `src/VvCash/Models/ParkedSaleSnapshot.cs` — полный снимок корзины (JSON payload).
- `src/VvCash/Services/IParkedSaleService.cs` — контракт сервиса.
- `src/VvCash/Services/ParkedSaleService.cs` — реализация.
- `src/VvCash/ViewModels/ParkedSalesViewModel.cs` — VM окна списка.
- `src/VvCash/Views/ParkedSalesWindow.axaml` (+ `.axaml.cs`) — окно списка.

**Правки:**
- `src/VvCash/Models/Product.cs` — `[JsonIgnore]` на `ImageBitmap`.
- `src/VvCash/Services/Data/IOfflineStorageService.cs` + `OfflineStorageService.cs` — таблица + CRUD `ParkedSales`.
- `src/VvCash/Services/ICartService.cs` + `CartService.cs` — `LoadSnapshot(...)`.
- `src/VvCash/ViewModels/PosViewModel.cs` — команды park/resume/open/delete, счётчик, модалка метки, предупреждение при закрытии смены.
- `src/VvCash/Views/PosView.axaml` — кнопка «Заказы»→«Отложенные» + бейдж, кнопка «Отложить», модалка метки, подтверждение закрытия смены.
- `src/VvCash/App.axaml.cs` — регистрация `IParkedSaleService` в DI.
- `src/VvCash/Assets/i18n/{ru,en,kk,uz,tg}.json` — новые ключи.

---

## Task 1: Модели снимка

**Files:**
- Create: `src/VvCash/Models/ParkedSaleSnapshot.cs`
- Create: `src/VvCash/Models/ParkedSale.cs`
- Modify: `src/VvCash/Models/Product.cs`

- [ ] **Step 1: Создать `ParkedSaleSnapshot.cs`**

```csharp
using System.Collections.Generic;
using VvCash.Models.Api;

namespace VvCash.Models;

/// <summary>Полный снимок корзины для отложенного чека (хранится как JSON в ParkedSale.Payload).</summary>
public class ParkedSaleSnapshot
{
    public List<ParkedCartItem> Items { get; set; } = new();
    public decimal ManualDiscountPercent { get; set; }
    public decimal ManualDiscountAmount { get; set; }
    public decimal CustomerDiscountPercent { get; set; }
    public List<Coupon> AppliedCoupons { get; set; } = new();
    public CounterpartyResponse? Customer { get; set; }
    public string? Label { get; set; }
}

public class ParkedCartItem
{
    public Product Product { get; set; } = null!;
    public int Quantity { get; set; }
}
```

- [ ] **Step 2: Создать `ParkedSale.cs`**

```csharp
using System;

namespace VvCash.Models;

/// <summary>Денормализованная строка списка отложенных чеков (для UI и БД).</summary>
public class ParkedSale
{
    public string Id { get; set; } = string.Empty;
    public string? Label { get; set; }
    public string? CustomerName { get; set; }
    public decimal Total { get; set; }
    public int ItemCount { get; set; }
    public DateTime CreatedAt { get; set; }
    public string Payload { get; set; } = string.Empty; // JSON ParkedSaleSnapshot
}
```

- [ ] **Step 3: В `Product.cs` исключить `ImageBitmap` из сериализации**

`Bitmap` не сериализуется в JSON. Forward атрибута на сгенерированное свойство через `[property: ...]`.

Заменить в `src/VvCash/Models/Product.cs`:

```csharp
    [ObservableProperty]
    private Bitmap? _imageBitmap;
```

на:

```csharp
    [ObservableProperty]
    [property: System.Text.Json.Serialization.JsonIgnore]
    private Bitmap? _imageBitmap;
```

- [ ] **Step 4: verify-build**

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```
Ожидаемо: `Build succeeded. 0 Error(s)`.

- [ ] **Step 5: Commit**

```powershell
git add src/VvCash/Models/ParkedSale.cs src/VvCash/Models/ParkedSaleSnapshot.cs src/VvCash/Models/Product.cs
git commit -m "feat: add parked sale snapshot models"
```

---

## Task 2: Таблица `ParkedSales` + CRUD в OfflineStorageService

**Files:**
- Modify: `src/VvCash/Services/Data/IOfflineStorageService.cs`
- Modify: `src/VvCash/Services/Data/OfflineStorageService.cs`

- [ ] **Step 1: Добавить методы в интерфейс**

В `src/VvCash/Services/Data/IOfflineStorageService.cs` добавить `using` и методы. Файл начинается с `using System.Collections.Generic; using System.Threading.Tasks; using VvCash.Models;` — добавить недостающее не нужно (`VvCash.Models` уже есть).

Вставить после строки `Task ClearUnsyncedDocumentsAsync();` (перед `Task InitializeAsync();`):

```csharp

    // Parked sales (отложенные чеки)
    Task SaveParkedSaleAsync(ParkedSale sale);
    Task<IEnumerable<ParkedSale>> GetParkedSalesAsync();
    Task<ParkedSale?> GetParkedSaleAsync(string id);
    Task DeleteParkedSaleAsync(string id);
```

- [ ] **Step 2: Создать таблицу в `InitializeAsync`**

В `src/VvCash/Services/Data/OfflineStorageService.cs`, внутри `command.CommandText = @"...";` блока в `InitializeAsync`, добавить определение таблицы перед строкой `-- Create indices for performance`:

```sql
            CREATE TABLE IF NOT EXISTS ParkedSales (
                Id TEXT PRIMARY KEY,
                Label TEXT,
                CustomerName TEXT,
                Total REAL NOT NULL,
                ItemCount INTEGER NOT NULL,
                CreatedAt TEXT NOT NULL,
                Payload TEXT NOT NULL
            );

```

(Вставить так, чтобы блок остался валидным SQL — между `);` таблицы `Products` и комментарием индексов.)

- [ ] **Step 3: Реализовать CRUD**

В `src/VvCash/Services/Data/OfflineStorageService.cs` добавить методы перед закрывающей `}` класса (после `ClearUnsyncedDocumentsAsync`):

```csharp

    public async Task SaveParkedSaleAsync(ParkedSale sale)
    {
        using var connection = new SqliteConnection(_connectionString);
        await connection.OpenAsync();

        using var command = connection.CreateCommand();
        command.CommandText = @"
            INSERT INTO ParkedSales (Id, Label, CustomerName, Total, ItemCount, CreatedAt, Payload)
            VALUES ($Id, $Label, $CustomerName, $Total, $ItemCount, $CreatedAt, $Payload)
            ON CONFLICT(Id) DO UPDATE SET
                Label=excluded.Label,
                CustomerName=excluded.CustomerName,
                Total=excluded.Total,
                ItemCount=excluded.ItemCount,
                CreatedAt=excluded.CreatedAt,
                Payload=excluded.Payload;
        ";
        command.Parameters.AddWithValue("$Id", sale.Id);
        command.Parameters.AddWithValue("$Label", (object?)sale.Label ?? DBNull.Value);
        command.Parameters.AddWithValue("$CustomerName", (object?)sale.CustomerName ?? DBNull.Value);
        command.Parameters.AddWithValue("$Total", sale.Total);
        command.Parameters.AddWithValue("$ItemCount", sale.ItemCount);
        command.Parameters.AddWithValue("$CreatedAt", sale.CreatedAt.ToString("o"));
        command.Parameters.AddWithValue("$Payload", sale.Payload);

        await command.ExecuteNonQueryAsync();
    }

    private ParkedSale ReadParkedSale(SqliteDataReader reader)
    {
        return new ParkedSale
        {
            Id = reader.GetString(0),
            Label = reader.IsDBNull(1) ? null : reader.GetString(1),
            CustomerName = reader.IsDBNull(2) ? null : reader.GetString(2),
            Total = reader.GetDecimal(3),
            ItemCount = reader.GetInt32(4),
            CreatedAt = DateTime.Parse(reader.GetString(5), null, System.Globalization.DateTimeStyles.RoundtripKind),
            Payload = reader.GetString(6)
        };
    }

    public async Task<IEnumerable<ParkedSale>> GetParkedSalesAsync()
    {
        var sales = new List<ParkedSale>();
        using var connection = new SqliteConnection(_connectionString);
        await connection.OpenAsync();

        using var command = connection.CreateCommand();
        command.CommandText = "SELECT Id, Label, CustomerName, Total, ItemCount, CreatedAt, Payload FROM ParkedSales ORDER BY CreatedAt DESC";

        using var reader = await command.ExecuteReaderAsync();
        while (await reader.ReadAsync())
        {
            sales.Add(ReadParkedSale(reader));
        }

        return sales;
    }

    public async Task<ParkedSale?> GetParkedSaleAsync(string id)
    {
        using var connection = new SqliteConnection(_connectionString);
        await connection.OpenAsync();

        using var command = connection.CreateCommand();
        command.CommandText = "SELECT Id, Label, CustomerName, Total, ItemCount, CreatedAt, Payload FROM ParkedSales WHERE Id = $Id LIMIT 1";
        command.Parameters.AddWithValue("$Id", id);

        using var reader = await command.ExecuteReaderAsync();
        if (await reader.ReadAsync())
        {
            return ReadParkedSale(reader);
        }

        return null;
    }

    public async Task DeleteParkedSaleAsync(string id)
    {
        using var connection = new SqliteConnection(_connectionString);
        await connection.OpenAsync();

        using var command = connection.CreateCommand();
        command.CommandText = "DELETE FROM ParkedSales WHERE Id = $Id";
        command.Parameters.AddWithValue("$Id", id);

        await command.ExecuteNonQueryAsync();
    }
```

- [ ] **Step 4: verify-build**

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```
Ожидаемо: `Build succeeded. 0 Error(s)`.

- [ ] **Step 5: Commit**

```powershell
git add src/VvCash/Services/Data/IOfflineStorageService.cs src/VvCash/Services/Data/OfflineStorageService.cs
git commit -m "feat: add ParkedSales table and CRUD to offline storage"
```

---

## Task 3: ParkedSaleService + DI

**Files:**
- Create: `src/VvCash/Services/IParkedSaleService.cs`
- Create: `src/VvCash/Services/ParkedSaleService.cs`
- Modify: `src/VvCash/App.axaml.cs`

- [ ] **Step 1: Создать `IParkedSaleService.cs`**

```csharp
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using VvCash.Models;

namespace VvCash.Services;

public interface IParkedSaleService
{
    /// <summary>Сохранить снимок корзины как отложенный чек. total — итоговая сумма к оплате на момент отложения.</summary>
    Task<ParkedSale> ParkAsync(ParkedSaleSnapshot snapshot, decimal total);

    Task<IReadOnlyList<ParkedSale>> GetAllAsync();

    /// <summary>Загрузить снимок и удалить запись (чек «вынут» в активную корзину). null — если не найдено.</summary>
    Task<ParkedSaleSnapshot?> ResumeAsync(string id);

    Task DeleteAsync(string id);
    Task<int> GetCountAsync();

    event EventHandler<int>? CountChanged;
}
```

- [ ] **Step 2: Создать `ParkedSaleService.cs`**

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Threading.Tasks;
using VvCash.Models;
using VvCash.Services.Data;

namespace VvCash.Services;

public class ParkedSaleService : IParkedSaleService
{
    private readonly IOfflineStorageService _storage;

    public event EventHandler<int>? CountChanged;

    public ParkedSaleService(IOfflineStorageService storage)
    {
        _storage = storage;
    }

    public async Task<ParkedSale> ParkAsync(ParkedSaleSnapshot snapshot, decimal total)
    {
        var sale = new ParkedSale
        {
            Id = Guid.NewGuid().ToString(),
            Label = string.IsNullOrWhiteSpace(snapshot.Label) ? null : snapshot.Label.Trim(),
            CustomerName = snapshot.Customer?.FullName,
            Total = total,
            ItemCount = snapshot.Items.Sum(i => i.Quantity),
            CreatedAt = DateTime.Now,
            Payload = JsonSerializer.Serialize(snapshot)
        };

        await _storage.SaveParkedSaleAsync(sale);
        await RaiseCountChangedAsync();
        return sale;
    }

    public async Task<IReadOnlyList<ParkedSale>> GetAllAsync()
    {
        var sales = await _storage.GetParkedSalesAsync();
        return sales.ToList();
    }

    public async Task<ParkedSaleSnapshot?> ResumeAsync(string id)
    {
        var sale = await _storage.GetParkedSaleAsync(id);
        if (sale == null) return null;

        ParkedSaleSnapshot? snapshot;
        try
        {
            snapshot = JsonSerializer.Deserialize<ParkedSaleSnapshot>(sale.Payload);
        }
        catch (JsonException)
        {
            // Битый payload — удаляем, чтобы не блокировать список.
            await _storage.DeleteParkedSaleAsync(id);
            await RaiseCountChangedAsync();
            return null;
        }

        await _storage.DeleteParkedSaleAsync(id);
        await RaiseCountChangedAsync();
        return snapshot;
    }

    public async Task DeleteAsync(string id)
    {
        await _storage.DeleteParkedSaleAsync(id);
        await RaiseCountChangedAsync();
    }

    public async Task<int> GetCountAsync()
    {
        var sales = await _storage.GetParkedSalesAsync();
        return sales.Count();
    }

    private async Task RaiseCountChangedAsync()
    {
        var count = await GetCountAsync();
        Avalonia.Threading.Dispatcher.UIThread.Post(() => CountChanged?.Invoke(this, count));
    }
}
```

- [ ] **Step 3: Зарегистрировать в DI**

В `src/VvCash/App.axaml.cs`, в `ConfigureServices`, после строки `services.AddSingleton<ICartService, CartService>();` добавить:

```csharp
        services.AddSingleton<IParkedSaleService, ParkedSaleService>();
```

- [ ] **Step 4: verify-build**

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```
Ожидаемо: `Build succeeded. 0 Error(s)`.

- [ ] **Step 5: Commit**

```powershell
git add src/VvCash/Services/IParkedSaleService.cs src/VvCash/Services/ParkedSaleService.cs src/VvCash/App.axaml.cs
git commit -m "feat: add ParkedSaleService and register in DI"
```

---

## Task 4: CartService.LoadSnapshot

**Files:**
- Modify: `src/VvCash/Services/ICartService.cs`
- Modify: `src/VvCash/Services/CartService.cs`

- [ ] **Step 1: Добавить метод в интерфейс**

В `src/VvCash/Services/ICartService.cs`, после строки `void ClearCustomerDiscount();` добавить:

```csharp
    void LoadSnapshot(
        IEnumerable<CartItem> items,
        decimal manualDiscountPercent, decimal manualDiscountAmount,
        decimal customerDiscountPercent,
        IEnumerable<Coupon> coupons);
```

- [ ] **Step 2: Реализовать в CartService**

В `src/VvCash/Services/CartService.cs` добавить метод перед `private void RaiseCartChanged()`:

```csharp
    public void LoadSnapshot(
        IEnumerable<CartItem> items,
        decimal manualDiscountPercent, decimal manualDiscountAmount,
        decimal customerDiscountPercent,
        IEnumerable<Coupon> coupons)
    {
        _items.Clear();
        _appliedCoupons.Clear();
        foreach (var item in items) _items.Add(item);
        foreach (var coupon in coupons) _appliedCoupons.Add(coupon);
        ManualDiscountPercent = manualDiscountPercent;
        ManualDiscountAmount = manualDiscountAmount;
        CustomerDiscountPercent = customerDiscountPercent;
        RaiseCartChanged();
    }
```

- [ ] **Step 3: verify-build**

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```
Ожидаемо: `Build succeeded. 0 Error(s)`.

- [ ] **Step 4: Commit**

```powershell
git add src/VvCash/Services/ICartService.cs src/VvCash/Services/CartService.cs
git commit -m "feat: add LoadSnapshot to cart service"
```

---

## Task 5: PosViewModel — команды и состояние

**Files:**
- Modify: `src/VvCash/ViewModels/PosViewModel.cs`

- [ ] **Step 1: Добавить зависимость на IParkedSaleService**

Добавить поле рядом с другими (после `private readonly ICounterpartyService _counterpartyService;`):

```csharp
    private readonly IParkedSaleService _parkedSaleService;
```

Добавить параметр в конструктор `PosViewModel(...)` — после `ICounterpartyService counterpartyService,` добавить `IParkedSaleService parkedSaleService,` (перед `HttpClient httpClient`). И присвоение в теле конструктора после `_counterpartyService = counterpartyService;`:

```csharp
        _parkedSaleService = parkedSaleService;
```

- [ ] **Step 2: Добавить observable-состояние**

После строки `public bool HasUnsyncedDocuments => UnsyncedDocumentsCount > 0;` добавить:

```csharp

    [ObservableProperty]
    [NotifyPropertyChangedFor(nameof(HasParkedSales))]
    private int _parkedSalesCount;
    public bool HasParkedSales => ParkedSalesCount > 0;

    // Park label modal
    [ObservableProperty] private bool _isParkLabelModalVisible = false;
    [ObservableProperty] private string _parkLabelInput = string.Empty;

    // Shift-close confirmation (when parked sales exist)
    [ObservableProperty] private bool _isShiftCloseConfirmVisible = false;
```

- [ ] **Step 3: Подписка на счётчик в InitializeAsync**

В методе `InitializeAsync`, после блока подписки на `_expenseDocumentService.UnsyncedDocumentsCountChanged` и установки `UnsyncedDocumentsCount`, добавить:

```csharp
        _parkedSaleService.CountChanged += (s, count) =>
        {
            Avalonia.Threading.Dispatcher.UIThread.Post(() =>
            {
                ParkedSalesCount = count;
            });
        };
        ParkedSalesCount = await _parkedSaleService.GetCountAsync();
```

- [ ] **Step 4: Хелпер сборки снимка**

Добавить приватный метод (например, перед командой `Pay`):

```csharp
    private ParkedSaleSnapshot BuildSnapshot(string? label) => new()
    {
        Items = _cartService.Items
            .Select(i => new ParkedCartItem { Product = i.Product, Quantity = i.Quantity })
            .ToList(),
        ManualDiscountPercent = _cartService.ManualDiscountPercent,
        ManualDiscountAmount = _cartService.ManualDiscountAmount,
        CustomerDiscountPercent = _cartService.CustomerDiscountPercent,
        AppliedCoupons = _cartService.AppliedCoupons.ToList(),
        Customer = SelectedCustomer,
        Label = label
    };
```

- [ ] **Step 5: Команды park / modal**

Добавить команды (рядом с другими `[RelayCommand]`):

```csharp
    [RelayCommand]
    private void OpenParkLabelModal()
    {
        if (!CartItems.Any()) return;
        ParkLabelInput = string.Empty;
        IsParkLabelModalVisible = true;
    }

    [RelayCommand]
    private void CloseParkLabelModal()
    {
        IsParkLabelModalVisible = false;
    }

    [RelayCommand]
    private async Task ConfirmParkSale()
    {
        if (!CartItems.Any()) { IsParkLabelModalVisible = false; return; }

        await _parkedSaleService.ParkAsync(BuildSnapshot(ParkLabelInput), TotalAmount);

        _cartService.ClearCart();
        _cartService.ClearCustomerDiscount();
        SelectedCustomer = null;
        _ = _customerDisplayService.ClearAsync();

        IsParkLabelModalVisible = false;
        StatusMessage = "Чек отложен.";
    }
```

- [ ] **Step 6: Команды открытия списка / возврата / удаления**

```csharp
    [RelayCommand]
    private async Task OpenParkedSales()
    {
        if (Avalonia.Application.Current?.ApplicationLifetime is Avalonia.Controls.ApplicationLifetimes.IClassicDesktopStyleApplicationLifetime desktop)
        {
            var mainWindow = desktop.MainWindow;
            if (mainWindow != null)
            {
                var dialog = new VvCash.Views.ParkedSalesWindow();
                dialog.DataContext = new ParkedSalesViewModel(dialog, _parkedSaleService);
                var result = await dialog.ShowDialog<object?>(mainWindow);
                if (result is string id)
                {
                    await ResumeParkedSale(id);
                }
            }
        }
    }

    private async Task ResumeParkedSale(string id)
    {
        // Если в корзине уже есть товары — авто-отложить текущую продажу.
        if (_cartService.Items.Any())
        {
            await _parkedSaleService.ParkAsync(BuildSnapshot(null), TotalAmount);
            _cartService.ClearCart();
            _cartService.ClearCustomerDiscount();
            SelectedCustomer = null;
        }

        var snapshot = await _parkedSaleService.ResumeAsync(id);
        if (snapshot == null) return;

        var items = snapshot.Items
            .Select(i => new CartItem { Product = i.Product, Quantity = i.Quantity })
            .ToList();

        _cartService.LoadSnapshot(
            items,
            snapshot.ManualDiscountPercent, snapshot.ManualDiscountAmount,
            snapshot.CustomerDiscountPercent,
            snapshot.AppliedCoupons);

        SelectedCustomer = snapshot.Customer;
        StatusMessage = "Отложенный чек возвращён.";
    }
```

- [ ] **Step 7: Предупреждение при закрытии смены**

Заменить существующую команду `CloseShiftAsync` так, чтобы при наличии отложенных она сначала спрашивала подтверждение. Найти метод:

```csharp
    [RelayCommand]
    private async Task CloseShiftAsync()
    {
        Console.WriteLine("[PosViewModel] CloseShiftAsync command executed.");
        System.Diagnostics.Debug.WriteLine("[PosViewModel] CloseShiftAsync command executed.");
        if (string.IsNullOrEmpty(CurrentShiftId)) return;

        IsLoadingShift = true;
        bool success = await _shiftService.CloseShiftAsync(CurrentShiftId);
        IsLoadingShift = false;
        if (success)
        {
            CurrentShiftId = null;
            IsShiftOpen = false;
            IsShiftModalVisible = true;
        }
    }
```

Заменить его на следующие три метода (триггер-команда + подтверждение + реальное закрытие):

```csharp
    [RelayCommand]
    private void CloseShift()
    {
        if (string.IsNullOrEmpty(CurrentShiftId)) return;

        if (ParkedSalesCount > 0)
        {
            IsShiftCloseConfirmVisible = true;
            return;
        }

        _ = DoCloseShiftAsync();
    }

    [RelayCommand]
    private async Task ConfirmCloseShift()
    {
        IsShiftCloseConfirmVisible = false;
        await DoCloseShiftAsync();
    }

    [RelayCommand]
    private void CancelCloseShift()
    {
        IsShiftCloseConfirmVisible = false;
    }

    private async Task DoCloseShiftAsync()
    {
        Console.WriteLine("[PosViewModel] DoCloseShiftAsync executed.");
        System.Diagnostics.Debug.WriteLine("[PosViewModel] DoCloseShiftAsync executed.");
        if (string.IsNullOrEmpty(CurrentShiftId)) return;

        IsLoadingShift = true;
        bool success = await _shiftService.CloseShiftAsync(CurrentShiftId);
        IsLoadingShift = false;
        if (success)
        {
            CurrentShiftId = null;
            IsShiftOpen = false;
            IsShiftModalVisible = true;
        }
    }
```

> Примечание: команда переименована `CloseShiftCommand` → `CloseShiftCommand` сохраняется (генерится из `CloseShift`). XAML-привязка `CloseShiftCommand` остаётся валидной (см. Task 7, где добавляются модалки).

- [ ] **Step 8: verify-build**

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```
Ожидаемо: `Build succeeded. 0 Error(s)`.

- [ ] **Step 9: Commit**

```powershell
git add src/VvCash/ViewModels/PosViewModel.cs
git commit -m "feat: add park/resume commands and shift-close warning to PosViewModel"
```

---

## Task 6: ParkedSalesViewModel + ParkedSalesWindow

**Files:**
- Create: `src/VvCash/ViewModels/ParkedSalesViewModel.cs`
- Create: `src/VvCash/Views/ParkedSalesWindow.axaml`
- Create: `src/VvCash/Views/ParkedSalesWindow.axaml.cs`

- [ ] **Step 1: Создать `ParkedSalesViewModel.cs`**

```csharp
using System.Collections.ObjectModel;
using System.Threading.Tasks;
using Avalonia.Controls;
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using VvCash.Models;
using VvCash.Services;

namespace VvCash.ViewModels;

public partial class ParkedSalesViewModel : ViewModelBase
{
    private readonly Window _window;
    private readonly IParkedSaleService _parkedSaleService;

    [ObservableProperty]
    [NotifyPropertyChangedFor(nameof(IsEmpty))]
    private ObservableCollection<ParkedSale> _sales = new();

    [ObservableProperty] private bool _isLoading = false;

    public bool IsEmpty => !IsLoading && Sales.Count == 0;

    public ParkedSalesViewModel(Window window, IParkedSaleService parkedSaleService)
    {
        _window = window;
        _parkedSaleService = parkedSaleService;
        _ = LoadAsync();
    }

    private async Task LoadAsync()
    {
        IsLoading = true;
        try
        {
            var sales = await _parkedSaleService.GetAllAsync();
            Sales = new ObservableCollection<ParkedSale>(sales);
        }
        finally
        {
            IsLoading = false;
        }
        OnPropertyChanged(nameof(IsEmpty));
    }

    [RelayCommand]
    private void Resume(ParkedSale sale)
    {
        _window.Close(sale.Id);
    }

    [RelayCommand]
    private async Task Delete(ParkedSale sale)
    {
        await _parkedSaleService.DeleteAsync(sale.Id);
        Sales.Remove(sale);
        OnPropertyChanged(nameof(IsEmpty));
    }

    [RelayCommand]
    private void Cancel()
    {
        _window.Close(null);
    }
}
```

- [ ] **Step 2: Создать `ParkedSalesWindow.axaml`**

Структура по образцу `CustomerSearchWindow.axaml`. Каждая строка: метка/имя клиента, кол-во+время, сумма, кнопки «Вернуть»/«Удалить».

```xml
<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:VvCash.Services"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:VvCash.ViewModels"
        xmlns:material="using:Material.Icons.Avalonia"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
        x:Class="VvCash.Views.ParkedSalesWindow"
        x:DataType="vm:ParkedSalesViewModel"
        Title="Parked Sales"
        Width="800" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        CornerRadius="12"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaChromeHints="NoChrome"
        ExtendClientAreaTitleBarHeightHint="-1">

    <Border Background="White" CornerRadius="12" ClipToBounds="True" BoxShadow="0 20 25 -5 #40000000">
        <Grid RowDefinitions="Auto,*,Auto">
            <!-- Header -->
            <Border Grid.Row="0" BorderBrush="{StaticResource Slate100Brush}" BorderThickness="0,0,0,1" Padding="32,24">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                        <material:MaterialIcon Kind="PauseCircleOutline" Width="36" Height="36" Foreground="{StaticResource PrimaryBrush}" />
                        <TextBlock Text="{Binding [ParkedSales], Source={x:Static services:I18nService.Instance}}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Slate900Brush}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Grid.Column="1" Classes="Transparent" Command="{Binding CancelCommand}" Padding="8" CornerRadius="99" Background="Transparent">
                        <material:MaterialIcon Kind="Close" Width="24" Height="24" Foreground="{StaticResource Slate500Brush}"/>
                    </Button>
                </Grid>
            </Border>

            <!-- List -->
            <Border Grid.Row="1" Padding="32,24">
                <Grid>
                    <TextBlock Text="{Binding [ParkedSalesEmpty], Source={x:Static services:I18nService.Instance}}"
                               IsVisible="{Binding IsEmpty}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="18" Foreground="{StaticResource Slate400Brush}"/>
                    <ListBox ItemsSource="{Binding Sales}" Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="vm:ParkedSalesViewModel">
                                <Border Background="{StaticResource Slate50Brush}" BorderBrush="{StaticResource Slate200Brush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,4">
                                    <Grid ColumnDefinitions="*, Auto, Auto">
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Label, FallbackValue='—'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Slate900Brush}" IsVisible="{Binding Label, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                            <TextBlock Text="{Binding CustomerName}" FontSize="14" Foreground="{StaticResource Slate600Brush}" IsVisible="{Binding CustomerName, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                            <TextBlock FontSize="13" Foreground="{StaticResource Slate500Brush}" Margin="0,4,0,0">
                                                <Run Text="{Binding ItemCount}"/><Run Text=" • "/><Run Text="{Binding CreatedAt, StringFormat='dd MMM HH:mm'}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat='{}{0:N2}'}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <Button Classes="PrimaryButton"
                                                    Command="{Binding $parent[ListBox].((vm:ParkedSalesViewModel)DataContext).ResumeCommand}"
                                                    CommandParameter="{Binding}">
                                                <TextBlock Text="{Binding [Resume], Source={x:Static services:I18nService.Instance}}"/>
                                            </Button>
                                            <Button Classes="OutlinedButton"
                                                    Command="{Binding $parent[ListBox].((vm:ParkedSalesViewModel)DataContext).DeleteCommand}"
                                                    CommandParameter="{Binding}">
                                                <material:MaterialIcon Kind="Delete" Width="18" Height="18" Foreground="{StaticResource Red500Brush}"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Footer -->
            <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource Slate100Brush}" BorderThickness="0,1,0,0" Padding="32">
                <Button Content="{Binding [Skip], Source={x:Static services:I18nService.Instance}}" Classes="OutlinedButton" Command="{Binding CancelCommand}" HorizontalAlignment="Stretch"/>
            </Border>
        </Grid>
    </Border>
</Window>
```

> Привязка к команде VM из `DataTemplate` идёт через `$parent[ListBox].((vm:ParkedSalesViewModel)DataContext).ResumeCommand` — стандартный Avalonia-приём, т.к. `DataContext` строки — это `ParkedSale`, а команда живёт на VM окна.

- [ ] **Step 3: Создать `ParkedSalesWindow.axaml.cs`**

```csharp
using Avalonia.Controls;

namespace VvCash.Views;

public partial class ParkedSalesWindow : Window
{
    public ParkedSalesWindow()
    {
        InitializeComponent();
    }
}
```

- [ ] **Step 4: verify-build**

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```
Ожидаемо: `Build succeeded. 0 Error(s)`.

- [ ] **Step 5: Commit**

```powershell
git add src/VvCash/ViewModels/ParkedSalesViewModel.cs src/VvCash/Views/ParkedSalesWindow.axaml src/VvCash/Views/ParkedSalesWindow.axaml.cs
git commit -m "feat: add parked sales list window"
```

---

## Task 7: PosView.axaml — кнопки и модалки

**Files:**
- Modify: `src/VvCash/Views/PosView.axaml`

- [ ] **Step 1: Оживить кнопку «Заказы» → «Отложенные» + бейдж**

Заменить строку [PosView.axaml:39]:

```xml
                    <Button Classes="NavButton" Content="{Binding [Orders], Source={x:Static services:I18nService.Instance}}" Margin="0,0,16,0" FontSize="14"/>
```

на:

```xml
                    <Button Classes="NavButton" Command="{Binding OpenParkedSalesCommand}" Margin="0,0,16,0" FontSize="14">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="{Binding [ParkedSales], Source={x:Static services:I18nService.Instance}}" VerticalAlignment="Center"/>
                            <Border Background="{StaticResource PrimaryBrush}" CornerRadius="99" Padding="6,1" MinWidth="20" IsVisible="{Binding HasParkedSales}">
                                <TextBlock Text="{Binding ParkedSalesCount}" Foreground="White" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Button>
```

- [ ] **Step 2: Добавить кнопку «Отложить» в зону действий корзины**

В блоке `StackPanel Grid.Column="1"` (строки ~261–274, где кнопки «Discount» и «ClearCart»), добавить кнопку «Отложить» между ними — после кнопки `OpenDiscountModalCommand` (после её закрывающего `</Button>` на строке ~267) вставить:

```xml
                                <Button Classes="SecondaryButton" Command="{Binding OpenParkLabelModalCommand}">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <material:MaterialIcon Kind="PauseCircleOutline" Width="18" Height="18" Foreground="{StaticResource Slate700Brush}"/>
                                        <TextBlock Text="{Binding [Park], Source={x:Static services:I18nService.Instance}}" FontSize="14"/>
                                    </StackPanel>
                                </Button>
```

- [ ] **Step 3: Добавить модалку метки отложения**

В конце файла, рядом с «Manual Discount Modal» (после её закрывающего `</Border>`), добавить новый оверлей. Найти `<!-- Manual Discount Modal -->` блок и **после** его внешнего закрывающего `</Border>` вставить:

```xml
        <!-- Park Label Modal -->
        <Border Grid.RowSpan="3" Background="#990f172a"
                IsVisible="{Binding IsParkLabelModalVisible}"
                ZIndex="2000">
            <Border Background="White" CornerRadius="24" Padding="32"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Width="480" BoxShadow="0 20 40 0 #40000000">
                <StackPanel Spacing="24">
                    <StackPanel Orientation="Horizontal" Spacing="16" HorizontalAlignment="Center">
                        <Border Background="{StaticResource Slate100Brush}" CornerRadius="12" Padding="12">
                            <material:MaterialIcon Kind="PauseCircleOutline" Width="32" Height="32" Foreground="{StaticResource Slate800Brush}"/>
                        </Border>
                        <TextBlock Text="{Binding [Park], Source={x:Static services:I18nService.Instance}}"
                                   FontSize="24" FontWeight="Black" Foreground="{StaticResource Slate900Brush}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBox Text="{Binding ParkLabelInput}"
                             Watermark="{Binding [ParkLabelHint], Source={x:Static services:I18nService.Instance}}"
                             FontSize="18" Padding="16"/>

                    <Grid ColumnDefinitions="*, *" Margin="0,8,0,0">
                        <Button Grid.Column="0" Classes="SecondaryButton" Margin="0,0,8,0" HorizontalAlignment="Stretch"
                                Content="{Binding [Skip], Source={x:Static services:I18nService.Instance}}"
                                Command="{Binding ConfirmParkSaleCommand}"/>
                        <Button Grid.Column="1" Classes="PrimaryButton" Margin="8,0,0,0" HorizontalAlignment="Stretch"
                                Content="{Binding [Park], Source={x:Static services:I18nService.Instance}}"
                                Command="{Binding ConfirmParkSaleCommand}"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Border>

        <!-- Shift Close Confirm Modal -->
        <Border Grid.RowSpan="3" Background="#990f172a"
                IsVisible="{Binding IsShiftCloseConfirmVisible}"
                ZIndex="2000">
            <Border Background="White" CornerRadius="24" Padding="32"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Width="480" BoxShadow="0 20 40 0 #40000000">
                <StackPanel Spacing="24">
                    <TextBlock Text="{Binding [ShiftCloseParkedWarning], Source={x:Static services:I18nService.Instance}}"
                               FontSize="20" FontWeight="Bold" Foreground="{StaticResource Slate900Brush}"
                               TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <Grid ColumnDefinitions="*, *">
                        <Button Grid.Column="0" Classes="SecondaryButton" Margin="0,0,8,0" HorizontalAlignment="Stretch"
                                Content="{Binding [Skip], Source={x:Static services:I18nService.Instance}}"
                                Command="{Binding CancelCloseShiftCommand}"/>
                        <Button Grid.Column="1" Classes="PrimaryButton" Margin="8,0,0,0" HorizontalAlignment="Stretch"
                                Command="{Binding ConfirmCloseShiftCommand}">
                            <TextBlock Text="{Binding [CloseShift], Source={x:Static services:I18nService.Instance}}"/>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>
        </Border>
```

> Кнопка закрытия смены в шапке привязана к `CloseShiftCommand` (она осталась с тем же именем — генерится из метода `CloseShift`). Дополнительных правок привязки не требуется.

- [ ] **Step 4: verify-build**

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```
Ожидаемо: `Build succeeded. 0 Error(s)`. (Ключи i18n добавим в Task 8 — до этого отсутствующие ключи покажут имя ключа, не ошибка сборки.)

- [ ] **Step 5: Commit**

```powershell
git add src/VvCash/Views/PosView.axaml
git commit -m "feat: wire park sale UI into POS view"
```

---

## Task 8: i18n ключи

**Files:**
- Modify: `src/VvCash/Assets/i18n/ru.json`
- Modify: `src/VvCash/Assets/i18n/en.json`
- Modify: `src/VvCash/Assets/i18n/kk.json`
- Modify: `src/VvCash/Assets/i18n/uz.json`
- Modify: `src/VvCash/Assets/i18n/tg.json`

- [ ] **Step 1: Добавить ключи в каждый файл**

Вставить пары ключ-значение (рядом с существующим ключом `"Orders"` в каждом файле; не забыть запятые в JSON). Значения по языкам:

`ru.json`:
```json
  "ParkedSales": "Отложенные",
  "Park": "Отложить",
  "Resume": "Вернуть",
  "Skip": "Пропустить",
  "ParkLabelHint": "Примечание (необязательно)",
  "ParkedSalesEmpty": "Нет отложенных чеков",
  "ShiftCloseParkedWarning": "Есть отложенные чеки. Закрыть смену?",
```

`en.json`:
```json
  "ParkedSales": "Parked",
  "Park": "Hold",
  "Resume": "Resume",
  "Skip": "Skip",
  "ParkLabelHint": "Note (optional)",
  "ParkedSalesEmpty": "No parked sales",
  "ShiftCloseParkedWarning": "There are parked sales. Close shift?",
```

`kk.json`:
```json
  "ParkedSales": "Кейінге қалдырылған",
  "Park": "Кейінге қалдыру",
  "Resume": "Қайтару",
  "Skip": "Өткізіп жіберу",
  "ParkLabelHint": "Ескертпе (міндетті емес)",
  "ParkedSalesEmpty": "Кейінге қалдырылған чектер жоқ",
  "ShiftCloseParkedWarning": "Кейінге қалдырылған чектер бар. Ауысымды жабу керек пе?",
```

`uz.json`:
```json
  "ParkedSales": "Keyinga qoldirilgan",
  "Park": "Keyinga qoldirish",
  "Resume": "Qaytarish",
  "Skip": "O‘tkazib yuborish",
  "ParkLabelHint": "Izoh (ixtiyoriy)",
  "ParkedSalesEmpty": "Keyinga qoldirilgan cheklar yo‘q",
  "ShiftCloseParkedWarning": "Keyinga qoldirilgan cheklar bor. Smenani yopilsinmi?",
```

`tg.json`:
```json
  "ParkedSales": "Ба таъхир",
  "Park": "Ба таъхир андохтан",
  "Resume": "Баргардонидан",
  "Skip": "Гузарондан",
  "ParkLabelHint": "Эзоҳ (ихтиёрӣ)",
  "ParkedSalesEmpty": "Чекҳои ба таъхирафтода нестанд",
  "ShiftCloseParkedWarning": "Чекҳои ба таъхирафтода ҳастанд. Навбатро бастан?",
```

> Если ключ `"CloseShift"` отсутствует в каком-то файле — добавить его тоже (используется в кнопке подтверждения): ru `"Закрыть смену"`, en `"Close shift"`, kk `"Ауысымды жабу"`, uz `"Smenani yopish"`, tg `"Навбатро бастан"`.

- [ ] **Step 2: Проверить валидность JSON**

```powershell
Get-ChildItem src/VvCash/Assets/i18n/*.json | ForEach-Object { $null = Get-Content $_.FullName -Raw | ConvertFrom-Json; "$($_.Name): OK" }
```
Ожидаемо: по строке `OK` на каждый файл, без исключений.

- [ ] **Step 3: verify-build**

```powershell
dotnet build src/VvCash/VvCash.csproj -c Debug -o "$env:TEMP\vvcash-verify"
```
Ожидаемо: `Build succeeded. 0 Error(s)`.

- [ ] **Step 4: Commit**

```powershell
git add src/VvCash/Assets/i18n/ru.json src/VvCash/Assets/i18n/en.json src/VvCash/Assets/i18n/kk.json src/VvCash/Assets/i18n/uz.json src/VvCash/Assets/i18n/tg.json
git commit -m "feat: add park sale i18n keys (ru/en/kk/uz/tg)"
```

---

## Task 9: Ручная верификация

**Files:** нет (только запуск).

> Закрыть запущенные экземпляры приложения перед обычной сборкой/запуском, иначе file lock.

- [ ] **Step 1: Запустить приложение**

```powershell
dotnet run --project src/VvCash/VvCash.csproj
```
Войти, открыть смену.

- [ ] **Step 2: Отложить чек**

Добавить 2–3 товара. Нажать «Отложить». В модалке ввести метку (напр. «Иван куртка») → «Отложить».
Ожидаемо: корзина очистилась, бейдж на «Отложенные» = 1.

- [ ] **Step 3: Отложить второй (без метки)**

Добавить товар, «Отложить» → «Пропустить» (пустая метка).
Ожидаемо: бейдж = 2.

- [ ] **Step 4: Персистентность**

Закрыть приложение, снова `dotnet run ...`.
Ожидаемо: бейдж «Отложенные» = 2 (данные в SQLite).

- [ ] **Step 5: Возврат**

Выбрать клиента со скидочной картой, добавить товар, применить ручную скидку, «Отложить».
Открыть «Отложенные» → «Вернуть» на этом чеке.
Ожидаемо: корзина, ручная скидка, клиент и его скидка восстановлены; запись исчезла из списка; бейдж уменьшился.

- [ ] **Step 6: Авто-отложение при возврате с непустой корзиной**

Добавить товар в корзину (не пусто). Открыть «Отложенные» → «Вернуть» другой чек.
Ожидаемо: текущая корзина авто-отложилась (появилась в списке при следующем открытии), выбранный чек загрузился.

- [ ] **Step 7: Удаление**

Открыть «Отложенные» → «Удалить» на строке.
Ожидаемо: строка исчезла, бейдж уменьшился.

- [ ] **Step 8: Предупреждение при закрытии смены**

При наличии отложенных нажать закрытие смены.
Ожидаемо: модалка «Есть отложенные чеки. Закрыть смену?». «Пропустить» — отмена (смена открыта). Закрытие — смена закрывается, отложенные остаются.

- [ ] **Step 9: Финальный коммит верификации (если были правки)**

Если в ходе верификации потребовались фиксы — закоммитить их отдельно с описанием найденного и исправленного.

---

## Self-review заметки

- **Покрытие спеки:** разделы 1–9 спеки → Tasks 1–9 соответственно (модели→T1, хранилище→T2, сервис→T3, корзина→T4, VM→T5, UI-окно→T6, UI-PosView→T7, i18n→T8, верификация→T9).
- **Сериализация:** `Product.ImageBitmap` помечен `[JsonIgnore]` (T1) — иначе сериализация снимка падает на `Bitmap`.
- **Имена команд:** `OpenParkedSalesCommand`, `OpenParkLabelModalCommand`, `ConfirmParkSaleCommand`, `CloseShiftCommand`, `ConfirmCloseShiftCommand`, `CancelCloseShiftCommand` — согласованы между T5 (генерация) и T7 (привязка).
- **Краевой случай:** битый JSON payload при возврате → запись удаляется, `null` (T3, `ResumeAsync`).
