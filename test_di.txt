Ah, so `HttpClient` is just resolved via `CreateClient("DefaultClient")` and added as Transient! That means any service that injects `HttpClient` will automatically get the correctly configured `DefaultClient`! This is great.
So I just need to add `services.AddSingleton<ICategoryService, CategoryService>();` and `CategoryService` just injects `HttpClient`.

The JSON response for categories has this structure:
{
  "body": [
    {
       "id": "guid",
       "name": "Cat1",
       ...
    }
  ]
}

So I will need a small DTO or wrap it in JsonDocument to extract the array, or use a generic wrapper:
public class ApiResponse<T> { public T Body { get; set; } }

Then I can deserialize into `ApiResponse<List<Category>>`. Wait, `body` could be a property. Let's look at `doc.json`. Yes, `body` is where the array of items is!

4. Update `PosViewModel.cs`:
- Remove `Categories` (string collection).
- Add `AllCategories` (Category collection).
- Add `QuickCategories` (Category collection).
- Add `Category? SelectedCategory`. Or keep `SelectedCategory` as string to not break bindings, or update `PosView.axaml` bindings. Since bindings in `PosView.axaml` are mostly `CommandParameter="{Binding}"`, they will pass `Category` instances.
- Update `SelectCategory` to handle `Category` type.
- What about `All`? If `SelectedCategory` is null, we are showing "All Categories".

Let's refine the UI interactions:
- When "ALL CATEGORIES" button is clicked -> `SelectCategoryCommand(null)` or pass a special "All" item.
- We show `ItemsControl` for categories if `SelectedCategory == null`.
- Otherwise, we show `ItemsControl` for products.

Wait, `IsCatalogOpen` determines if the pane is open. The pane should show `Products` or `AllCategories`.

```xml
<ScrollViewer Grid.Row="1">
    <ItemsControl ItemsSource="{Binding AllCategories}" IsVisible="{Binding IsViewingCategories}">
        ...
    </ItemsControl>
    <ItemsControl ItemsSource="{Binding Products}" IsVisible="{Binding !IsViewingCategories}">
        ...
    </ItemsControl>
</ScrollViewer>
```
