So the plan should be:
1. Update `IProductService.cs` and `MockProductService.cs`
   - Change `GetCategoriesAsync()` to return a list of `Category` models.
   - Add `GetQuickCategoriesAsync()` to return quick categories.
2. Create `Category.cs` in `Models/`.
   - Should have `Id`, `Name`. The mock can return "All" as one of the quick categories if needed, or we just hardcode an "All" button in the UI and then a list of dynamically fetched quick categories below it.
3. Update `PosViewModel.cs`:
   - Add `[ObservableProperty] private ObservableCollection<Category> _quickCategories = new();`
   - Add `[ObservableProperty] private ObservableCollection<Category> _allCategories = new();`
   - Note: The current view passes strings as `CommandParameter` to `SelectCategoryCommand`. We should pass `Category` or string (Id).
   - `SelectedCategory` can remain as `Category` object.
   - Wait, if "All" is a category, we can just represent it with a dummy Category object (Id="", Name="ALL").
4. Let's see how `SelectCategoryCommand` works. It expects a string currently.
   - `private async Task SelectCategory(string category)` - we can change it to `(Category category)`.
   - The UI can bind to `{Binding}` (which is a Category).
5. Let's look at `PosView.axaml`. We will replace the hardcoded `Button`s inside the `StackPanel` under `<!-- Sidebar -->` with an `ItemsControl` bound to `QuickCategories`.
   - Also, the prompt mentions "we have categories that we need to show not in all categories but as a list where we currently have several". So there should be a place where we show ALL categories. Where is "All categories"?
   - Oh, I see! "all categories" might be an extra drawer or maybe they just mean "not in the full list of categories, but as a list where we currently have several (the sidebar)". So the user is referring to the sidebar! The sidebar *is* the place where we have several categories right now.
   - So the user wants:
     1. Fetch *all* categories from `/cashes/category`
     2. Fetch *sidebar* categories from `/cashes/category/show-on-cash`
     3. The sidebar should display categories from `/cashes/category/show-on-cash`.
     4. Where do we show all categories? Maybe the user wants an "All Categories" button that opens a drawer or changes the main view? Actually, currently `IsCatalogOpen` opens the drawer with *Products* for the selected category.
     5. If we have to show ALL categories, we could add a "More Categories" button that replaces the product grid in the drawer with a list of categories? Or maybe a separate dialog? The user didn't specify where to show all categories, they just said "we have categories that we need to show not in all categories but as a list where we currently have several".
     6. Wait. "all categories нужно брать отсюда, так же у нас есть категории которые нужно показывать в не в all categories а списком где у нас сейчас стоят несколько"
     Translation: "all categories need to be taken from here, also we have categories that need to be shown not in all categories but as a list where we currently have several"
     Meaning:
     - All categories from `http://market.proffi.io/swagger/index.html#/cashes/get_cashes_category_`
     - The ones to show as a list *where we currently have several* (i.e. the sidebar) from `http://market.proffi.io/swagger/index.html#/cashes/get_cashes_category_show_on_cash_`.
     So we need an "All categories" view! Or at least, the "All Categories" button in the sidebar could open a drawer showing the full list of categories instead of products? No, currently "All" means "all products". Wait. "not in all categories but as a list..." Oh! They probably mean: the sidebar (where we currently have several) should show the "show-on-cash" categories. And then there's an "All Categories" button that shows the REST of them, or ALL of them?
     Let's clarify the UI.
     Currently we have:
     - ALL (ViewGrid icon) - shows all products
     - CLOTHING
     - SHOES
     - ACCS
     - SALE

     If we make "All Categories" an item in the sidebar, clicking it could open a modal or the drawer, showing all categories. Let's look at how "All" was functioning.
     `await LoadProductsAsync("All");`
     So "All" fetched all products.
     If we want an "All Categories" view, we can replace "All" with a button that shows a grid of categories, or maybe we just add a "More Categories" button? Let's re-read carefully:
     "все категории нужно брать отсюда, так же у нас есть категории которые нужно показывать в не в all categories а списком где у нас сейчас стоят несколько"
     "all categories need to be taken from here, also we have categories that need to be shown not in all categories but as a list where we currently have several"
     This means: we have an "All categories" place (probably a section or dialog or view) and a "Quick list" (the sidebar).
     Where is "all categories" place? Wait, maybe "all categories" translates to "all categories" in the UI? We don't have "all categories" in the UI. We only have "ALL" products.
     Maybe we should add an "All Categories" button at the bottom of the sidebar, which opens a grid of categories in the SplitView? Let's check `PosView.axaml` again.
