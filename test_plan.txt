1. We need to create a new model `Category` in `src/VvCash/Models/` which contains `Id`, `Name`, `Color`, `Icon` (or whatever the API returns, but `Id` and `Name` at least).
2. We need to update `IProductService` to fetch these categories from the API. We'll add two methods: `GetCategoriesAsync()` which calls `/api/cashes/category` and `GetPosCategoriesAsync()` which calls `/api/cashes/category/show-on-cash`. Wait, actually maybe it's just `Task<IEnumerable<Category>> GetCategoriesAsync()` and `Task<IEnumerable<Category>> GetQuickAccessCategoriesAsync()`.
3. The current `GetCategoriesAsync()` returns `IEnumerable<string>`. We should change it to `IEnumerable<Category>`.
4. We need to update `MockProductService` to return `Category` objects.
5. In `PosViewModel`, we will store `Categories` as `ObservableCollection<Category>` and `QuickCategories` as `ObservableCollection<Category>`. We will also need to update `SelectedCategory` to hold a `Category` object instead of a string, or just hold the `CategoryId`.
6. In `PosView.axaml`, we will render the QuickCategories dynamically in the sidebar instead of hardcoded buttons.
7. Also we need to render the "All Categories" list dynamically if there is a way to see all categories. The user said "we have categories that we need to show not in all categories but as a list where we currently have several". So the sidebar has quick categories. There should be a way to see "all categories". Currently there's no UI for "all categories" other than the sidebar. Wait! There's an "All" button in the sidebar. Let's look at the current UI.
