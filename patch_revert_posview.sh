# For some reason my patch script in step 6 did not apply the PosView update correctly! Wait, I modified `temp.axaml` and then somehow the `mv temp3.axaml` got lost or failed silently because my build script probably failed and I reverted it?
# Let's fix it properly.
